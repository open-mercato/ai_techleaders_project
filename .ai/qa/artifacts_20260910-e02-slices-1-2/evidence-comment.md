🤖 om-auto-qa-pr completed: **PASS**

Verified the invitation acceptance and single-use token flow, deadline onboarding state, profile validation/recovery, draft saving, readiness, shared preview, publish/copy/unpublish/stable-republish behavior, signed-out allowlisted public page, and desktop/mobile layouts in a real browser against ephemeral PostgreSQL.

Automated checks on the exact PR head are also green: Build, Lint, Unit tests, and Integration tests.

The repository is private, so GitHub's raw evidence URLs may require an authenticated session to render. Local evidence is retained under `.ai/qa/artifacts_20260910-e02-slices-1-2/` and is not part of the product branch.
